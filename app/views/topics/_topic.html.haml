%tr.topic
  %td
    -if topic.locked?
      %i.fa.fa-lock
    =link_to topic.subject, topic_path(topic)
  %td
    =topic.exp
  %td
    =render "shared/cliq", cliq: topic.cliq
  -if @admin
    %td
      =topic.reports