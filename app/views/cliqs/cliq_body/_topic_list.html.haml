%table.table.table-striped
  %thead
    %tr
      %th
        Subject
      %th
        Interest
      %th
        Category
      -if can? :update, @cliq && current_page?(admin_cliq_path(@cliq))
        %th
          Reports
  %tbody
    -topics.each do |t|
      %tr
        %td
          =link_to t.subject, topic_path(t)
        %td
          =t.exp
        %td
          =render "shared/cliq", desc: t.cliq
        -if can? :update, @cliq && current_page?(admin_cliq_path(@cliq))
          %td
            =t.reports
